GlobalStyles {
	Rule '.card' {
		width = fit_content,
		max_width = pct(100),
		padding = rem(1),
		background_color = var 'bg_raised',
		border_left = { var 'ornament_thickness', solid, var 'fg_sep' },
	}
}
